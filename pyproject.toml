[project]
name = "chessdashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.0",
    "dlt[chess]>=1.0",
    "python-chess>=1.10",
    "python-dotenv>=1.0",
]
